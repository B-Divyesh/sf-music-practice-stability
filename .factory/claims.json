[
  {
    "id": "sample-improvement",
    "claim": "The sample shows six sessions and a 52% drop in timing spread",
    "where": "landing first screen and demo trend",
    "test": "npm test -- --grep @claim:sample-improvement",
    "sandbox": "fresh browser context at /demo; assert six seeded rows, latest 26 ms, and 52% lower summary"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit",
    "where": "landing facts and README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context at /demo; wait for service worker, set offline, reload, and assert the seeded passage"
  },
  {
    "id": "local-only",
    "claim": "Audio and practice history stay on this device",
    "where": "landing facts, limits, privacy page, and README",
    "test": "npm test -- --grep @claim:local-only",
    "sandbox": "fresh browser context at /demo; monitor the entire sample interaction and assert every request is same-origin"
  },
  {
    "id": "tap-capture",
    "claim": "Records six repeated takes and measures timing spread",
    "where": "product interface and README",
    "test": "npm test -- --grep @claim:tap-capture",
    "sandbox": "fresh /demo session; record four taps in each of six takes, save, and assert a measured spread appears"
  },
  {
    "id": "input-options",
    "claim": "Accepts microphone, Web MIDI, and tap input",
    "where": "landing steps, practice input selector, and README",
    "test": "npm test -- --grep @claim:input-options",
    "sandbox": "fresh /demo session with a fake microphone and MIDI input; start microphone capture and record a full MIDI take"
  },
  {
    "id": "csv-export",
    "claim": "Exports practice history as CSV",
    "where": "practice data controls and README",
    "test": "npm test -- --grep @claim:csv-export",
    "sandbox": "fresh /practice page; export and assert the CSV header"
  },
  {
    "id": "data-backup",
    "claim": "Exports and imports a JSON backup and clears local history",
    "where": "practice data controls, privacy page, and README",
    "test": "npm test -- --grep @claim:data-backup",
    "sandbox": "fresh /practice page; create a passage, export and import its JSON, then confirm clear and assert the empty state"
  },
  {
    "id": "free-passage-limit",
    "claim": "The free version saves one passage",
    "where": "landing facts, paid section, and README",
    "test": "npm test -- --grep @claim:free-passage-limit",
    "sandbox": "fresh /practice page; save one passage and assert the second-passage action explains the full version"
  },
  {
    "id": "paid-passages",
    "claim": "A valid full-version license saves more than one passage",
    "where": "paid section and README",
    "test": "npm test -- --grep @claim:paid-passages",
    "sandbox": "fresh browser context with a fixture verify response; activate a license and save three passages"
  },
  {
    "id": "controlled-takes",
    "claim": "You can mark a take as controlled",
    "where": "landing steps and practice take controls",
    "test": "npm test -- --grep @claim:controlled-takes",
    "sandbox": "fresh /demo session; capture a take, mark it controlled, save six takes, and assert the saved controlled count"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo mode never reads or writes real practice data",
    "where": "README and demo banner",
    "test": "npm test -- --grep @claim:demo-isolation",
    "sandbox": "fresh browser context; create real data, change demo data, then assert each mode retains only its own data"
  },
  {
    "id": "audio-not-recorded",
    "claim": "Audio is analysed in memory and is not recorded",
    "where": "landing limits, privacy page, and README",
    "test": "npm test -- --grep @claim:audio-not-recorded",
    "sandbox": "fresh /demo session with fake microphone; begin and cancel microphone capture, assert no MediaRecorder is created and no audio file or network request is made"
  },
  {
    "id": "take-correction",
    "claim": "You can correct any take before saving",
    "where": "landing limits",
    "test": "npm test -- --grep @claim:take-correction",
    "sandbox": "fresh /demo session; capture a take, remove it, recapture it, and save the six-take session"
  },
  {
    "id": "license-on-demand",
    "claim": "License verification contacts Sociobot only after you enter or buy a license",
    "where": "privacy page",
    "test": "npm test -- --grep @claim:license-on-demand",
    "sandbox": "fresh browser context; assert no external request before license submission, then fixture-submit a token and observe the Sociobot verify request"
  },
  {
    "id": "revoked-license",
    "claim": "A refunded or revoked license stops full-version access",
    "where": "terms page",
    "test": "npm test -- --grep @claim:revoked-license",
    "sandbox": "fresh browser context; activate a valid fixture license, expire its cached verdict, return a revoked fixture, and assert full access is removed"
  }
]
